<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:background="#FFFFFF"
    android:padding="8dp"
    android:layout_marginBottom="8dp"
    android:elevation="2dp">

    <!-- Hình ảnh sản phẩm -->
    <ImageView
        android:id="@+id/img_checkout_product"
        android:layout_width="64dp"
        android:layout_height="64dp"
        android:scaleType="centerCrop"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"
        android:src="@drawable/ic_launcher_foreground" />

    <!-- Tên sản phẩm -->
    <TextView
        android:id="@+id/tv_checkout_name"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:text="Tên sản phẩm"
        android:textSize="16sp"
        android:textStyle="bold"
        android:textColor="#212121"
        app:layout_constraintStart_toEndOf="@id/img_checkout_product"
        app:layout_constraintTop_toTopOf="@id/img_checkout_product"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintHorizontal_bias="0.0"
        android:layout_marginStart="12dp" />

    <!-- Giá sản phẩm -->
    <TextView
        android:id="@+id/tv_checkout_price"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:text="100.000₫"
        android:textSize="14sp"
        android:textColor="#D32F2F"
        app:layout_constraintStart_toStartOf="@id/tv_checkout_name"
        app:layout_constraintTop_toBottomOf="@id/tv_checkout_name"
        app:layout_constraintEnd_toEndOf="parent" />

</androidx.constraintlayout.widget.ConstraintLayout>
